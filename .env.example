# Required for Photo OCR and Card Ladder screenshot OCR fallback.
GOOGLE_API_KEY=

# Photo OCR reads detected card crops concurrently.
# Use 1 for slower/lower API pressure, 3 for the default, 4 for the fastest supported setting.
LUCAS_PHOTO_OCR_CROP_WORKERS=3

# Dense photos keep looking for this many slab regions before skipping extra detector passes.
# Higher is better for 2-row grid photos, lower is faster for small batches.
LUCAS_PHOTO_OCR_REGION_TARGET=10

# Required for complete setup. Enables Google Sheets rule/payout files in Company Rules.
# Create an OAuth Client ID with application type "Desktop app" in Google Cloud.
GOOGLE_SHEETS_OAUTH_CLIENT_ID=
GOOGLE_SHEETS_OAUTH_CLIENT_SECRET=

# Required for complete setup. Point this at your WORKING SHEETS folder.
# If this is set, INCOMING SHEETS, RECEIVED SHEETS, and sheet_markers.json live beside it.
# Example: G:\My Drive\CARD_PIPELINE\WORKING SHEETS
LUCAS_WORKING_SHEETS_DIR=

# Legacy fallback for setups with folders under one pipeline root.
# Example: G:\My Drive\CARD_PIPELINE
LUCAS_PIPELINE_DIR=

# Optional Windows CourtYard Scanner automation. LUCAS can launch the configured
# emulator and local Appium server on demand after they have been installed once.
LUCAS_CY_APPIUM_ENABLED=0
LUCAS_CY_AUTO_START=1
LUCAS_CY_APPIUM_URL=http://127.0.0.1:4723
LUCAS_CY_APPIUM_COMMAND=
LUCAS_CY_ANDROID_SDK=
LUCAS_CY_ANDROID_AVD=LUCAS_Courtyard
LUCAS_CY_ANDROID_UDID=
LUCAS_CY_APP_ACTIVITY=.MainActivity
LUCAS_CY_EMULATOR_START_TIMEOUT=180
LUCAS_CY_APPIUM_START_TIMEOUT=45
LUCAS_CY_LOOKUP_TIMEOUT=30

# Optional eBay listing trial. Keep EBAY_ENV=sandbox until Sandbox listing,
# withdrawal, republishing, and reconciliation have been verified.
EBAY_ENV=sandbox
EBAY_CLIENT_ID=
EBAY_CLIENT_SECRET=
EBAY_RUNAME=
EBAY_OAUTH_SCOPES=https://api.ebay.com/oauth/api_scope https://api.ebay.com/oauth/api_scope/sell.account https://api.ebay.com/oauth/api_scope/sell.inventory

# Desktop LUCAS connects through the eBay broker. For a fully local trial,
# start ebay_broker_server.py and use http://127.0.0.1:8788/ebay. The eBay
# developer keyset's RuName must return to that broker's /ebay/callback route.
LUCAS_EBAY_BROKER_URL=http://127.0.0.1:8788/ebay
LUCAS_EBAY_BROKER_PUBLIC_URL=http://127.0.0.1:8788/ebay
LUCAS_EBAY_BROKER_STATE_SECRET=
LUCAS_EBAY_ALLOWED_CALLBACK_HOSTS=lucas.mikeyscards.com,team-lucas.mikeyscards.com

# Live publishing is blocked even with EBAY_ENV=production unless this is set
# to 1 for an explicitly approved production listing.
EBAY_ALLOW_PRODUCTION_LISTINGS=0
